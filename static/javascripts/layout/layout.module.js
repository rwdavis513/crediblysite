(function() {
    'use strict';

    angular
        .module('credibly.layout', [
            'credibly.layout.controllers'
        ]);

     angular
        .module('credibly.layout.controllers',[]);

})();